[mysqld]
# This settings should only be enabled while debugging some kind of error in queries from the client
# https://dev.mysql.com/doc/refman/8.0/en/query-log.html
general_log = on
general_log_file = /var/log/app/mysqld/general.log